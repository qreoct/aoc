#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    freopen("../in/03.txt", "r", stdin);

    int x;

    while (cin >> x) {
        // handle x
    }
}