#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    freopen("../in/01a.txt", "r", stdin);

    int x;

    while (cin >> x) {
        // handle x
    }
}